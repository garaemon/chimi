(defsystem chimi
    :name "chimi"
    :depends-on (log4cl)
    :components (;;(:file "util")
		 (:file "chimi")
                 ;;(:file "log" :depends-on ("util"))
		 ;;(:file "timer" :depends-on ("util"))
		 ))
